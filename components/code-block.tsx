// import { CopyBlock, obsidian, atomOneDark, atomOneLight } from "react-code-blocks";

interface CodeBlockProps {
  text: string;
  language: string;
  showlineNumbers?: boolean;
  wrapLines?: boolean;
}

export default function CodeBlock(props: CodeBlockProps) {
  return (<></>);
}
